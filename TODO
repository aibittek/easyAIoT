1、音频接口统一
初始化接口；
开始、停止接口；
录音数据回调接口；
反初始化接口；

2、AI调用接口统一
初始化接口；
连接接口（HTTP、Websocket）；
通信回调接口；
提供不同的回调执行判断逻辑：例如连接成功、结束、可读、可写、错误等；

3、COMMON接口的统一
md5、sha256等统一接口

以上接口参考python之类的接口，并提供接口文档和例子，做成document网页版本