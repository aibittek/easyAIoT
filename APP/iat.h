#ifndef __IAT_H
#define __IAT_H

/**
 * @brief 语音转写
 * @param  appid            应用appid
 * @param  key              应用apikey
 * @param  secret           应用secret
 */
void iat(const char *appid, const char *key, const char *secret);

#endif